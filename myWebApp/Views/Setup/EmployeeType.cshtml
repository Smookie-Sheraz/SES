@*@model IEnumerable<Entities.Models.EmployeeType>*@
@*<div class="main-panel">
   <div class="content-wrapper">
          <div class="row">

            <div class="col-12 grid-margin stretch-card">
              <div class="card">
                <div class="card-body">
                  <h4 class="card-title">Add Employee Type</h4>
                  <p class="card-description">
                    The Employee Type can be added here!
                  </p>
                  <div class="table-responsive">
                            @if (Model.Any())
                            {
                               <table class="table table-hover">
                               <thead>
                                <tr>
                                  <th>Actions</th>
                                  <th>Id</th>
                                  <th>Code</th>
                                  <th>Description</th>
                                  <th>Short Description</th>
                                  <th>Active</th>
                                </tr>
                              </thead>
                              <tbody>
                                        @foreach (var Employee in Model)
                                        {
                                             <tr>
                                                         <td class="actions">
                                                           <a class="fa fa-edit mr-3" asp-route-id="@Employee.EmployeeTypeId" asp-controller="Setup" asp-action="UpdateEmployeeType" role="button"></a>
                                                           <a class="fa fa-trash text-danger" onclick="return confirm('Are you sure you want to delete?');" asp-route-id="@Employee.EmployeeTypeId" asp-controller="Setup" asp-action="DeleteEmployeeType" role="button"></a>
                                                            </td>
                                              <td>@Employee.EmployeeTypeId</td>
                                              <td>@Employee.Code</td>
                                              <td>@Employee.Description</td>
                                              <td>@Employee.ShortDescripiton</td>
                                              <td>
                                                    @if (Employee.IsActive)
                                                    {
                                                      <label class="badge badge-success">Active</label>
                                                    }
                                                    else
                                                    {
                                                      <label class="badge badge-danger">InActive</label>
                                                    }
                                              </td>
                                            </tr> 
                                        }
                              </tbody>
                            </table>
                            }
                            else
                            {
                              <h3>No Data Found!</h3>
                            }
                  </div>
                </div>
              </div>
            </div>
            <div class="col-2 offset-10">
              <a class="btn btn-primary" asp-controller="Setup" asp-action="AddEmployeeType" role="button">Add</a>
            </div>
          </div>
        </div>
</div>*@