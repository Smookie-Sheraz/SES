@*@model IEnumerable<Entities.Models.School>*@
@*<div class="main-panel">
   <div class="content-wrapper">
          <div class="row">

            <div class="col-12 grid-margin stretch-card">
              <div class="card">
                <div class="card-body">
                  <h4 class="card-title">Add School</h4>
                  <p class="card-description">
                    The School can be added here!
                  </p>
                  <div class="table-responsive">
                                @if (Model.Any())
                                {
                               <table class="table table-hover">
                               <thead>
                                <tr>
                                  <th>Id</th>
                                  <th>Code</th>
                                  <th>Description</th>
                                  <th>Location</th>
                                  <th>Active</th>
                                </tr>
                              </thead>
                              <tbody>
                                            @foreach (var school in Model)
                                            {
                                            @foreach (var location in ViewBag.Locations)
                                            {
                                             <tr>
                                              <td>@school.SchoolId</td>
                                              <td>@school.SchoolCode</td>
                                              <td>@school.Description</td>
                                              <td>@location.Description</td>
                                              <td>
                                                        @if (school.IsActive)
                                                        {
                                                          <label class="badge badge-success">Active</label>
                                                        }
                                                        else
                                                        {
                                                         <label class="badge badge-danger">InActive</label>
                                                        }
                                              </td>
                                              <td>
                                                     <a class="btn btn-primary" asp-route-id="@school.SchoolId" asp-controller="School" asp-action="UpdateSchool" role="button">Update</a>
                                                                     <a class="btn btn-primary" asp-route-id="@school.SchoolId" asp-controller="School" asp-action="DeleteSchool" role="button">Delete</a>
                                              </td>
                                            </tr> 
                                            }
                                  }
                              </tbody>
                            </table>
                                }
                                else
                                {
                              <h3>No Data Found!</h3>
                                }
                  </div>
                </div>
              </div>
            </div>
            <div class="col-2 offset-10">
              <a class="btn btn-primary" asp-controller="School" asp-action="AddSchool" role="button">Add</a>
            </div>
          </div>
        </div>
</div>*@